{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.2.0"
  },
  "dependencies": {
    "@trpc/client": "^10.11.0",
    "@trpc/server": "^10.11.0",
    "prisma": "^4.10.1",
    "trpc-nuxt": "^0.6.0",
    "zod": "^3.20.6"
  }
}
